#include <stdio.h>
int execute_multiply(int a, int b) {
    return a * b;
}

int main() 
{
    int operand1 = 20;
    int operand2 = 12;
    int result;
    result = execute_multiply(operand1, operand2);
    printf("Multiplying %d and %d: Result = %d\n", operand1, operand2, result);
    return 0;
}
